import express from 'express';
// import User form '../models/user.model.js';
const router = express.Router()

router.get('/:id', async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
    }
})

export default router;